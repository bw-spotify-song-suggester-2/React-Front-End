import {
    LOGIN_START,
    LOGIN_SUCCESS,
    LOGIN_ERROR,
    SIGNUP_START,
    SIGNUP_SUCCESS,
    SIGNUP_ERROR,
} from '../actions';

const initialState = {
    username: '',
    password: ''
}

const reducer = (state = initialState, action) => {

}

export default reducer;